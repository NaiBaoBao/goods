spring:
  application:
    name: Goods
  datasource:
    url: jdbc:mysql://localhost:3306/oomall?serverTimezone=UTC&useUnicode=true&characterEncoding=UTF-8&userSSL=false
    username: root
    password: 12345678
mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true

pagehelper:
  helper-dialect: mysql
  reasonable: true
  support-methods-arguments: true
  params: count=countsql
server:
  port: 8017
#eureka:
#  client:
#   serviceUrl:
#      defaultZone: http://localhost:8011/eureka/
web:
  path: /Users/catherinewang/Desktop/picture

